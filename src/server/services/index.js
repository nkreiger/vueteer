/**
 * @module services
 * @description Returns all general service modules for easy access
 * @requires services/svc-pptr
 * @returns {Object} {pptr}
 */
/* Custom Dependencies */
const pptr = require('./svc-pptr');

module.exports = {
    pptr
};
