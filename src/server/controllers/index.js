/**
 * @module controllers
 * @description Returns all general controller modules for easy access
 * @requires controllers/pptr
 * @returns {Object} {users}
 */
/* Custom Dependencies */
const pptr = require('./pptr');

module.exports = {
    pptr
};
